framework:
    cache:
        pools:
            # Cache específico pra TV — TTL curto, dados voláteis
            tv.cache:
                adapter: cache.adapter.filesystem
                default_lifetime: 60
