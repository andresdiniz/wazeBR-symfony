const normalizeTrendDay = (value) => { if (typeof value === 'string') return value.includes('T') ? value.split('T')[0] : value.split(' ')[0]; if (typeof value === 'number' && Number.isFinite(value)) { const ms = value > 100000000000 ? value : value * 1000; return new Date(ms).toISOString().slice(0, 10); } return ''; };

// Use normalizeTrendDay(item.day) in renderTrendChart before formatting labels.