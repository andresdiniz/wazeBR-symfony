{% extends 'base.html.twig' %}

{% block title %}API Docs{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-12">
            <h1 class="display-5 mb-4">Documentaç°µo da API</h1>
            
            <div class="card mb-4">
                <div class="card-body">
                    <h5 class="card-title">Autenticaç°µo</h5>
                    <p class="card-text">Todas as requisiç°µes à API devem incluir um token de autenticaç°µo no header:</p>
                    <pre><code>Authorization: Bearer YOUR_API_TOKEN</code></pre>
                </div>
            </div>

            <div class="card mb-4">
                <div class="card-body">
                    <h5 class="card-title">Endpoints</h5>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">
                            <strong>GET /api/alerts</strong><br>
                            <small>Lista todos os alertas do partner autenticado</small>
                        </li>
                        <li class="list-group-item">
                            <strong>GET /api/traffic-jams</strong><br>
                            <small>Lista todos os engarrafamentos do partner autenticado</small>
                        </li>
                        <li class="list-group-item">
                            <strong>POST /api/webhooks/waze</strong><br>
                            <small>Recebe webhooks do Waze</small>
                        </li>
                    </ul>
                </div>
            </div>

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Exemplo de Uso</h5>
                    <pre><code>curl -X GET "https://api.acheireviews.com.br/api/alerts" \
  -H "Authorization: Bearer YOUR_API_TOKEN"</code></pre>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
