        
        <!-- Security: No inline event handlers, use event listeners instead -->
        <script nonce="{{ csp_nonce('script-src') }}">
            // Prevent clickjacking
            if (self !== top) {
