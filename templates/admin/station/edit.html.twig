{% extends 'base.html.twig' %}
{% block title %}Editar Estação CEMADEN{% endblock %}
{% block content %}
<div class="page-header" style="display:flex;align-items:center;gap:var(--space-4)">
  <a href="{{ path('admin_station_index') }}" class="btn btn-ghost btn-sm">
    <i data-lucide="arrow-left" style="width:14px;height:14px"></i> Voltar
  </a>
  <h1 class="page-title">Editar Estação</h1>
</div>
<div class="card" style="max-width:580px">
  <div style="padding:var(--space-6)">
    <form method="post">
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:var(--space-4)">
        <div class="form-group">
          <label class="form-label" for="cod_estacao">Código da Estação</label>
          <input class="form-control" id="cod_estacao" name="cod_estacao" required value="{{ station.cod_estacao }}">
        </div>
        <div class="form-group">
          <label class="form-label" for="uf">UF</label>
          <input class="form-control" id="uf" name="uf" maxlength="2" required value="{{ station.uf }}">
        </div>
      </div>
      <div class="form-group">
        <label class="form-label" for="nome">Nome da Estação</label>
        <input class="form-control" id="nome" name="nome" required value="{{ station.nome }}">
      </div>
      <div class="form-group">
        <label class="form-label" for="municipio">Município</label>
        <input class="form-control" id="municipio" name="municipio" required value="{{ station.municipio }}">
      </div>
      <div class="form-group">
        <label class="form-label" for="partner_slug">Parceiro</label>
        <select class="form-control" id="partner_slug" name="partner_slug" required>
          <option value="">Selecione…</option>
          {% for p in partners %}
            <option value="{{ p.slug }}" {{ station.partner_slug == p.slug ? 'selected' }}>{{ p.name }} ({{ p.slug }})</option>
          {% endfor %}
        </select>
      </div>
      <button type="submit" class="btn btn-primary" style="width:100%">Salvar Alterações</button>
    </form>
  </div>
</div>
{% endblock %}
