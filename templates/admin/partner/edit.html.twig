{% extends 'base.html.twig' %}

{% block title %}Editar Parceiro{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Editar Parceiro</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-primary">
                Salvar Alterações
            </button>
        {{ form_end(form) }}

        <div class="mt-3">
            <a href="{{ path('app_partner_admin_index') }}" class="btn btn-secondary">
                Voltar à lista
            </a>
        </div>
    </div>
{% endblock %}
