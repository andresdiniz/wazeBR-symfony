<aside class="sidebar">
    <div class="sidebar-header">
        <span class="brand">WazeBR</span>
    </div>
    <nav class="sidebar-nav">
        <ul class="dashboard-nav">
            <li class="dashboard-nav-item">
                <a href="{{ path('dashboard') }}"
                   class="dashboard-nav-link {{ currentRoute == 'dashboard' ? 'is-active' : '' }}"
                   {% if currentRoute == 'dashboard' %}aria-current="page"{% endif %}>
                    <i class="bi bi-speedometer2"></i>
                    <span>Dashboard</span>
                </a>
            </li>
            <li class="dashboard-nav-item">
                <a href="{{ path('alert_index') }}"
                   class="dashboard-nav-link {{ currentRoute == 'alert_index' ? 'is-active' : '' }}"
                   {% if currentRoute == 'alert_index' %}aria-current="page"{% endif %}>
                    <i class="bi bi-exclamation-triangle"></i>
                    <span>Alertas</span>
                </a>
            </li>
            <li class="dashboard-nav-item">
                <a href="{{ path('traffic_jam_index') }}"
                   class="dashboard-nav-link {{ currentRoute == 'traffic_jam_index' ? 'is-active' : '' }}"
                   {% if currentRoute == 'traffic_jam_index' %}aria-current="page"{% endif %}>
                    <i class="bi bi-car-front"></i>
                    <span>Congestionamentos</span>
                </a>
            </li>
        </ul>
    </nav>
</aside>
